House Price PredictionThis project is a Python-based data science workflow that analyzes a housing dataset and implements Supervised Machine Learning Regressors to predict house prices based on various real estate features (such as bedrooms, bathrooms, square footage, and location).📋 Table of ContentsOverviewDataset SpecificationsProject WorkflowDependencies & InstallationModel Evaluation Strategy🔍 OverviewThe notebook guides you through an end-to-end Machine Learning pipeline:Data Loading & Inspection: Reading and exploring the structural composition of the dataset.Exploratory Data Analysis (EDA): Visualizing target variable distributions.Data Preprocessing: Checking for missing values and preparing features.Model Selection: Implementing multiple regression algorithms to discover the best fit.📊 Dataset SpecificationsThe project utilizes a dataset containing 4,600 rows and 18 distinct features (data.csv). Below is a detailed breakdown of the features included:Feature NameData TypeDescriptiondateObjectDate the house was listed/soldpriceFloat64Target Variable: The sale price of the housebedroomsFloat64Number of bedroomsbathroomsFloat64Number of bathroomssqft_livingInt64Square footage of the interior living spacesqft_lotInt64Square footage of the land lotfloorsFloat64Total number of floorswaterfrontInt64Dummy variable (1 if property has a waterfront view, 0 otherwise)viewInt64Rating index of the property's view qualityconditionInt64Overall condition rating of the housesqft_aboveInt64Square footage of house space above ground levelsqft_basementInt64Square footage of the basement interioryr_builtInt64The year the house was initially builtyr_renovatedInt64The year the house was renovated (0 if never renovated)streetObjectStreet address of the propertycityObjectCity where the house is locatedstatezipObjectZip code and State indicatorcountryObjectCountry location🛠️ Project Workflow1. Library ImportsThe core structure loads standard numerical analysis, visualization, and Scikit-Learn modeling libraries:Pythonimport pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns

from sklearn.model_selection import train_test_split
from sklearn.linear_model import LinearRegression
from sklearn.ensemble import RandomForestRegressor, GradientBoostingRegressor
from sklearn.metrics import mean_absolute_error, mean_squared_error
2. Data Health CheckEvaluates non-null data counts across features.Runs a completeness check via df.isnull().sum(), which confirms 0 missing values present within the base dataset.3. Exploratory DistributionPlots a distribution histogram of the target feature (price) using Seaborn's histplot to detect skewness, variations, and outlying data points across real estate prices.💻 Dependencies & InstallationTo run this notebook seamlessly, make sure your Python environment has the following packages installed:Bashpip install pandas numpy matplotlib seaborn scikit-learn
📐 Model Evaluation StrategyThe project sets up the metrics needed to thoroughly evaluate and compare the regression algorithms (Linear Regression, Random Forest, and Gradient Boosting).The performance of each model is measured using standard statistical error equations:Mean Absolute Error (MAE) measures the average magnitude of errors in a set of predictions, without considering their direction:$$MAE = \frac{1}{n} \sum_{i=1}^{n} |y_i - \hat{y}_i|$$Mean Squared Error (MSE) accentuates larger errors by squaring the differences before averaging them:$$MSE = \frac{1}{n} \sum_{i=1}^{n} (y_i - \hat{y}_i)^2$$(Where $y_i$ represents the actual price, $\hat{y}_i$ represents the predicted price, and $n$ represents the total number of tested samples).
